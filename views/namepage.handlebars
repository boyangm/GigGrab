<div id="namepage">
    <div id="picContainer">
        <img src={{user.image}} alt="profile pic">
    </div>
    <div id= "namepageContent">
        <h3>{{user.name}}</h3>
        <h3>{{user.instrument}}</h3>
        <div class="stats">
        <i class="material-icons star">star</i>
        <h3>{{user.rating}}</h3>
        </div> 
        <h6>{{user.bio}}</h6>
    </div>
</div>